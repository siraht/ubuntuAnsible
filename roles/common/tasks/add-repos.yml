---
- name: Add common repo keys w/ shell
  become: true
  shell:
    cmd: "{{ item }}"
    warn: false
  loop: "{{ repokeys }}"

- name: Add common repos
  become: true
  apt_repository:
    repo: "{{ item }}"
    state: present
    update_cache: false
  loop: "{{ repos }}"
